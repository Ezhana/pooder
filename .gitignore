# --- 依赖管理 ---
node_modules
.pnpm-debug.log*

# --- 构建产物 (所有包下的 dist 目录) ---
dist
build
out
*.tsbuildinfo

# --- 日志文件 ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# --- 编辑器 & IDE (建议忽略个人配置) ---
.vscode/*
!.vscode/extensions.json  # 允许共享推荐插件
!.vscode/settings.json    # 允许共享项目特定设置
!.vscode/launch.json      # 允许共享调试配置
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- 操作系统 ---
.DS_Store
Thumbs.db
desktop.ini

# --- 测试 & 覆盖率 ---
coverage
__snapshots__
.nyc_output

# --- 环境配置文件 (严禁上传) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# --- TypeScript ---
# 如果使用项目引用 (Project References)，通常不忽略 tsbuildinfo，
# 但在普通的 Monorepo 中通常会生成在 dist 或根部，建议忽略。
*.tsbuildinfo

# --- 缓存 ---
.eslintcache
.stylelintcache
.prettiercache
.parcel-cache
.cache
.vite
.turbo        # 如果以后使用 Turborepo
.vercel       # 如果部署到 Vercel

# --- 临时文件 ---
*.bak
*.tmp
*.temp

# --- 某些框架特定 ---
.svelte-kit
.next
.nuxt